import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config optmodule.exports = {
  allowedDevOrigins: ['local-origin.dev', '*.local-origin.dev'],
}ions here */
  reactCompiler: true,
};

export default nextConfig;
